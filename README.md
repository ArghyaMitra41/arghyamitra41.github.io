After editing run the following commands to install dependencies and start the development server:

	npm install
	npm start

To deploy website, run:

	npm run build
	npm run deploy

See it live at https://arghyamitra41.github.io
